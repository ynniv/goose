[package]
name = "goose-server"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description.workspace = true

[lints]
workspace = true

[dependencies]
goose = { path = "../goose" }
goose-mcp = { path = "../goose-mcp" }
rmcp = { workspace = true }
axum = { version = "0.8.1", features = ["ws", "macros"] }
tokio = { workspace = true }
chrono = "0.4"
tower-http = { version = "0.5", features = ["cors"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = { workspace = true, features = ["preserve_order"] }
futures = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json", "time"] }
tracing-appender = "0.2"
tokio-stream = "0.1"
anyhow = { workspace = true }
bytes = "1.5"
http = "1.0"
base64 = "0.21"
config = { version = "0.15.13", features = ["toml"] }
thiserror = "1.0"
clap = { version = "4.4", features = ["derive"] }
serde_yaml = "0.9.34"
utoipa = { version = "4.1", features = ["axum_extras", "chrono"] }
reqwest = { version = "0.12.9", features = ["json", "rustls-tls", "blocking", "multipart", "stream"], default-features = false }
tokio-util = "0.7.15"
# Local speech-to-text (Nemotron)
# Use load-dynamic to avoid build-time ONNX Runtime download (which needs native-tls)
# Runtime is downloaded separately via UI
parakeet-rs = { version = "0.2", default-features = false, features = ["cpu", "load-dynamic"] }
ort = { version = "2.0.0-rc.11", default-features = false, features = ["load-dynamic"] }
symphonia = { version = "0.5", default-features = false, features = ["wav", "pcm", "mkv"] }
symphonia-adapter-libopus = "0.2"
async-stream = "0.3"
flate2 = "1.0"
tar = "0.4"
serde_path_to_error = "0.1.20"
tokio-tungstenite = { version = "0.28.0", features = ["rustls-tls-native-roots"] }
url = "2.5.7"
rand = "0.9.2"
hex = "0.4.3"
socket2 = "0.6.1"
fs2 = "0.4.3"
rustls = { version = "0.23", features = ["ring"] }
uuid = { version = "1.19.0", features = ["v4"] }

[target.'cfg(windows)'.dependencies]
winreg = { version = "0.55.0" }

[[bin]]
name = "goosed"
path = "src/main.rs"

[[bin]]
name = "generate_schema"
path = "src/bin/generate_schema.rs"

[dev-dependencies]
tower = "0.5"
async-trait = "0.1.89"
tempfile = "3.15.0"
env-lock = { workspace = true }
wiremock = { workspace = true }
